
import { randomDec } from 'codefest/helpers/random-dec';
import { module, test } from 'qunit';

module('Unit | Helper | random dec');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = randomDec([42]);
  assert.ok(result);
});

