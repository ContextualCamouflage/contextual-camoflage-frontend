
import { randomLoc } from 'codefest/helpers/random-loc';
import { module, test } from 'qunit';

module('Unit | Helper | random loc');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = randomLoc([42]);
  assert.ok(result);
});

