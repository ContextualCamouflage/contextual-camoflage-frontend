
import { random } from 'codefest/helpers/random';
import { module, test } from 'qunit';

module('Unit | Helper | random');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = random([42]);
  assert.ok(result);
});

